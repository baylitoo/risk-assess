agent: critic
can_read:
  - artifact.*
can_write:
  - artifact.critique
cannot:
  - read:artifact.document_corpus
  - read:document.raw.*
  - write:artifact.risk_register   # the critic challenges; it never edits the register itself
  - execute:ticket.create
  - execute:report.publish
